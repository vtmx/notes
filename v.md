# vlang

Adiciona link:

```bash
sudo ./v symlink
```

Remove link:

```bash
unlink v
```

Executa:

```bash
v run hello.v
```

Hot-reload:

```bash
v watch run hello.v
```

Vigia:

```bash
v watch hello.v
```

Compila:

```bash
v hello.v
./hello
```
